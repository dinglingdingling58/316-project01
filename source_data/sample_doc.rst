示例 RST 文档
=============

这是一个 reStructuredText 格式的示例文档，用于测试 RST 解析功能。

二级标题
--------

RST 使用下划线来标记标题层级。

三级标题
~~~~~~~~

RST 支持多种标记符号：

- ``=`` 用于一级标题
- ``-`` 用于二级标题
- ``~`` 用于三级标题
- ``^`` 用于四级标题

四级标题
^^^^^^^^

指令示例
--------

.. note::
   这是一个 note 指令。

.. warning::
   这是一个 warning 指令。

链接示例
--------

这是一个 `链接到示例 <https://example.com>`_。

表格示例
--------

+--------+--------+
| 列 1   | 列 2   |
+========+========+
| 数据 1 | 数据 2 |
+--------+--------+
| 数据 3 | 数据 4 |
+--------+--------+
